interface:
  display_name: "Crayon"
  short_description: "Clear explanations matched to your knowledge"
  default_prompt: "Use $crayon to explain this directly in plain language, with the detail needed to answer my question."

policy:
  allow_implicit_invocation: true
